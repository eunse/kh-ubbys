<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	
	<!-- 전체 댓글 수 조회 SQL -->
	<entry key="getReplyListCount">
		SELECT COUNT(*) FROM qna_reply_list WHERE reply_status='Y' AND qna_post_id = ?
	</entry>

	<!-- 댓글 목록 조회 -->
	<entry key="selectList">
		SELECT reply_id, reply_content, reply_like, reply_date, qna_post_id, user_id, user_nickname
		FROM qna_reply
		JOIN "USER" USING(user_id)
		WHERE reply_status = 'Y'
		AND qna_post_id = ?
		ORDER BY reply_id
	</entry>
	
	<!-- 댓글 삽입 -->
	<entry key="insertReply">
		INSERT INTO qna_reply
		VALUES(SEQ_QRNO.NEXTVAL, ?, DEFAULT, DEFAULT, DEFAULT, ?, ?)
	</entry>
	
	<!-- 댓글 수정 -->
	<entry key="updateReply">
		UPDATE qna_reply SET
		reply_content = ?
		WHERE reply_id = ?
	</entry>

	<!-- 댓글 삭제 -->
	<entry key="deleteReply">
		UPDATE qna_reply SET
		reply_status = 'N'
		WHERE reply_id = ?
	</entry>
	
	<!-- QnaReply 좋아요 SQL -->
	<entry key="likeQnaReply">
		UPDATE qna_reply SET
		reply_like = ?
		WHERE reply_id = ?
	</entry>
	
	<!-- 내 QnaReply 목록 조회 -->
	<entry key="myReplyList">
		SELECT * FROM qna_reply_list
		WHERE user_id = ? AND reply_status='Y'
		ORDER BY reply_id
	</entry>

</properties>
